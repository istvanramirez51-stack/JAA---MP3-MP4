{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "bash scripts/install-ytdlp.sh && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
